/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['images.unsplash.com', 'img.icons8.com', 'storyset.com', 'cdn.sanity.io'],
    }
}

module.exports = nextConfig
